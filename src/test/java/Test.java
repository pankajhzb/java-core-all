

@Test
public class Test {
    // assertTrhows( UserValidException.class, () -> {})

    @InjectMock
    Servie

    @Mock
    EployeRepository emprepo

            @Mock
    AcooutDetails actDetails

when(emprepo.getValue(Mockityo.any(), Mockito.anyString())).thenReturn()
                    Servie.getOffer();

    // scheduled jobs
    //every 5 min to clear log
    //TestNG
    //RestAssured.when


}
